// 2. Напишите программу, которая выводит
//случайное число из отрезка [10, 99] и показывает наибольшую цифру числа.
//78 -> 8
//12-> 2
//85 -> 8

int TakeNum(int num)
{
                    int result1 = num % 10;
                    int result2 = num / 10;
                    if (resolt1 > result2) ;
                    {
                                        return resolt1;
                    }
                   else
                    {
                                        return resolt2;
                    }
}
int itog = TakeNum(new Random().Next(10, 100));
Console.WritLine(itig);

