// 1. Напишите программу, которая принимает на вход
//координаты точки (X и Y), причём X ≠ 0 или Y ≠ 0 и
//выдаёт номер четверти плоскости, в которой находится
//эта точка.

void Qvarters(int x? int y);
{
                    if (x == 0 || y == 0)
                                        Console.WritLine("x==0, y==0");
                    else if (x>0 && y>0)
                    Console.WritLine ("1");
                    else if (x<0 && y>0)
                    Console.WritLine ("2");

}