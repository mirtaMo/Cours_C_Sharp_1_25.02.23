// Задача 1: Напишите программу, которая по
//заданному номеру четверти, показывает диапазон
//возможных координат точек в этой четверти (x и y).

void Qvarters(int num);
{
                    if (num == 1)
                                        Console.WritLine("x>0 && y>0");
                    else if (num == 2)
                                        Console.WritLine(x > 0 && y > 0);
                    else if (num == 3)
                                        Console.WritLine(x < 0 && y < 0);
                    else if (num == 4)
                                        Console.WritLine(x > 0 && y < 0);
                    else
                                        Console.WritLine(err);

}

int num1 = int.Parse (Console.WritLine()!);

Qvarters (num1);